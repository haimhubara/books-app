export const ErrorMessage = ({message}) => {
    return (
        <section>
            <p className="text-2xl text-center font-semibold dark:text-slate-100 my-10  ">{message}</p>
        </section>
    )
}
